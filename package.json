{
  "name": "ha-tankpriser-tests",
  "private": true,
  "description": "Test harness for the Tankpriser Lovelace card. Not shipped to Home Assistant — the integration itself has no build step and no npm dependencies.",
  "scripts": {
    "test": "node tests/card.test.js && node tests/map.test.js"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
